# 交接状态 · 2026-08-12

用于冷启动恢复工作。**先读这一页，再读 [`CLAUDE.md`](./CLAUDE.md)。**

---

## 一句话状态

**M0–M4 全部完成，17 个 issue 全关。** `docker compose up` 起来就是一个能用的
产品；主链路（说一句话 → 拿到小队 → 全员确认 → 共域诞生 → 留下记录 →
下次被引用）在真栈上验证过。

| | 数量 |
|---|---|
| 后端测试 | 388 |
| 前端测试 | 95 |
| API 路径 | 41 |
| 数据表 | 20（全部 RLS + FORCE） |
| 迁移 | 12 |
| ADR | 5 |

实测：两万人漏斗 16ms · 语义召回七倍富集 · 求解 40 候选 6 组中位 0.082s ·
稳定性 5.4ms · 播种 1.3s。

---

## 还没做完的

- **UI-only autoresearch 没跑。** 量表 `pnpm e2e:persona-walkthrough` 建好了，
  但它只覆盖五项指标里机器能判的三项（`blocked_steps` / `term_leaks` /
  `missing_states`）；`confusion_hits` 与 `abandon_points` 要浏览器里的 agent
  走一遍
- 表结构缺口（子智能体逐条报过，都写在对应 issue 的关闭评论里）：
  关系切面需多人共同确认、切面有效期、`contested` 状态、完整导出与
  「删除全部数据」
- `next_steps` 里「转给社团」没有对应端点

---

## 这一轮抓到的静默 bug，值得记住的模式

它们的共同点是：**测试全绿，日志正常，而用户那一屏永远是空的。**

1. **清算从不给成员开待答复** —— 提案产出了，但没人能点「我加入」。
   两层各自的测试都绿，没有一条跨过它们
2. **「给定种子完全可复现」一直是假的** —— 课表用了内置 `hash()`，
   而可复现性测试在同一个进程里比较，永远相等
3. **「缺个会剪辑的」抽成 `会剪辑`** —— 词表里只有 `剪辑`，这条需求
   永远匹配不到人，不报错
4. **兜底截止期拿窗口对齐基准当"现在"** —— 落在过去，提案一生成就过期
5. **没有 `.dockerignore`** —— 宿主 `.venv` 与 `__pycache__` 被 COPY 进镜像

第 1 条只有端到端走一遍才能发现。这是**分层测试的系统性盲区**：
每一层都测了自己，没人测两层之间那根线。

---


## 已完成

| 产出 | 位置 |
|---|---|
| 领域语言与 10 条不变量 | [`CONTEXT.md`](./CONTEXT.md) |
| 赛题理解（唯一外部约束来源） | [`docs/01-赛题理解.md`](./docs/01-赛题理解.md) |
| 产品设计 | [`docs/02-产品设计.md`](./docs/02-产品设计.md) |
| 技术架构 | [`docs/03-技术架构.md`](./docs/03-技术架构.md) |
| 治理与安全 | [`docs/04-治理与安全.md`](./docs/04-治理与安全.md) |
| 研究依据与**评审记录** | [`docs/05-研究依据与评审.md`](./docs/05-研究依据与评审.md) |
| 仿真与测试人口 | [`docs/06-仿真与测试人口.md`](./docs/06-仿真与测试人口.md) |
| 产品语言与界面原则 | [`docs/07-产品语言与界面原则.md`](./docs/07-产品语言与界面原则.md) |
| 前端规格（每屏数据契约） | [`docs/08-前端规格.md`](./docs/08-前端规格.md) |
| PRD（163 条用户故事） | [`docs/prd/01-共域平台.md`](./docs/prd/01-共域平台.md) |
| 16 个 tracer bullet issue | GitHub `xinzhuwang-wxz/ai-social-connect`（全部关闭） |
| 开局必读（红线、命令、派子智能体的边界） | [`CLAUDE.md`](./CLAUDE.md) |
| 后端全部实现 | `apps/api/src/cofield/` |
| 前端六屏 | `apps/web/app/`、`apps/web/components/` |
| 一条命令起全套 | `docker-compose.yml` + 两个 Dockerfile |
| 播种命令 | `pnpm seed`（两万人 1.3 秒） |
| CI 三个 job | `.github/workflows/ci.yml` |

---

## 恢复后的第一步

先读 [`CLAUDE.md`](./CLAUDE.md)，再读 [`docs/GOAL.md`](./docs/GOAL.md) 的
「完成的定义」三节。

**下一件事是跑 UI-only autoresearch**（配置见 GOAL.md 末节）。八个 persona
只通过界面走全链路，任一步卡住都算未通过。这一关**必须能失败**——
如果它从来没判过不合格，说明量表设松了。

```
docker compose up          # 起来就有两万人的校园
pnpm check                 # 测试 + 类型 + 领域纯度 + 界面词汇 + 契约无漂移
pnpm e2e:persona-walkthrough
```
```
gh issue view 1
```

---

## Issue 依赖图

```
#1  地基：Clock 端口、合成主体与仿真租户          ← 无阻塞，第一个做
├── #2  意图编译
│   └── #3  匹配信封与代理授权
│       └── #5  合成校园人口与漏斗前两段  ← 也依赖 #2 #4
│           └── #6  成组：求解 + 稳定性检查 + 成局证明
│               ├── #7  窗口撮合（也依赖 #1）
│               ├── #8  互惠视角与受限协商
│               │   └── #9  确认门与原子成局
│               │       └── #10 共域
│               │           ├── #11 场域智能体（必须证明没弄坏 #10）
│               │           └── #12 行动回声
│               │               └── #13 自我管理面
│               │                   └── #14 回流  ← 也依赖 #7，闭环在这里合上
│               └── #15 凑不出队时
└── #4  行动机会（组织者发布）

#16 念头暂存 ← #2 #4（漏斗最前段，可与主链并行）
```

**#14 是"钱在哪"的那一片。** 前面 13 片都在铺路，如果只能演示一片，是它。

---

## 已经吵完的事，别重开

这些判断在本轮被推翻过至少一次，理由都写在 [`docs/05`](./docs/05-研究依据与评审.md) §5（R1–R11）。恢复会话时如果又想到这些，先去看为什么被否掉：

| 别再提议 | 一句话理由 |
|---|---|
| 引入 Graphiti | 核心卖点是 LLM 从文本自动抽取实体关系，恰是本系统禁止的能力 |
| 引入 Neo4j / FalkorDB / 独立图库 | 查询全是 2–3 跳浅层；引入即打破"Postgres 唯一权威" |
| Mem0 做主记忆层 | scope 只有 user/session/agent，没有 relationship/collective |
| 迁到 Qdrant | 高 ACL 选择性恰恰是 pgvector 的舒适区，不是风险场景 |
| 做校园地图 / 虚拟校园 | 需要新增隐私红线才能安全，价值配不上代价（R10） |
| 用 mock/stub 做测试接缝 | 会测到自己造的假货。**唯一被替换的是"人"**（R11） |
| 全面禁止 AI 代聊 | **已放松**。代聊三档可配置，红线只剩"AI 不能产生承诺"（05 §6.5） |
| 自创同意 schema | 有 ISO/IEC TS 27560:2023，标准复用与开源复用是同一条红线 |
| 做可浏览的个人主页 | 会退回"看人卡决定连不连接"，冷启动用户天然吃亏 |
| 即时撮合 | deadline 可识别时窗口清算优于提速（JPE 2020） |
| 把稳定性检查降级为优化项 | 硬门槛。不稳定的组会在第一次线下见面前散架 |

---

## 待你决定

| # | 问题 | 影响 |
|---|---|---|
| 1 | **截止期谎报用哪种对策**——"声明后不可缩短" vs "谎报成本体现为可选组变少" | 意图 schema 与撮合策略，做 #7 之前要定 |
| 2 | **稳定性检查用哪个概念**——Nash 近似够不够，还是要 individual/core | #6 的核心，且时延未验证 |
| 3 | **单校还是多校起步** | pgvector 边界与 OpenFGA 租户模型 |
| 4 | **产品对外叫什么** | "共域/CoField" 是内部代号，抽象，需用户测试 |
| 5 | `agent_reply_policy` 默认取哪档 | 影响 #8 与合规评估 |
| 6 | 产品副标题 | 参照 Tara 的"帮你聊 帮你找"，候选见 07 §9 |

另有四项 schema 冻结前必须完成的研究，见 [`docs/04`](./docs/04-治理与安全.md) §9。

---

## 三个最脆弱的地方

**稳定性检查没有工程先例。** 容量护栏里那个 1 秒是估计值。这是全系统唯一一个既新增、又没有成熟工程参照的环节。若压测扛不住需退到更弱的稳定性概念（如只检查 top-k），**这会改变成局证明里"没有人有更好的去处"那句承诺的措辞**——产品与工程要一起决定，不能工程单方面降级。

**截止期谎报没有对策，且会随规模互相学会。**

**组织者供给是冷启动单点，而且不是技术风险。** 第一批增长完全依赖拿下几个院系或赛事方；拿不下来，学生侧自发需求撑不起流动性，漏斗第一段空转。

---

## 环境

- 远端：`https://github.com/xinzhuwang-wxz/ai-social-connect`（`origin/main`）
- `gh` 已认证为 `xinzhuwang-wxz`，仓库 issues 已启用
- 标签：`ready-for-agent`、`tracer-bullet`
- 无 CI、无代码、无 ADR
- 一手依据：分享会转写 `/Users/bamboo/.codex/attachments/7ec73f15-9e54-48f6-9020-af16943fe875/pasted-text.txt`

---

## 未做的事

- **Tara 实际界面未查看**（Chrome 扩展未连接）。08 的视觉语言部分是推断，拿到截图后需校准
- issue 之外的任务拆分（16 片可再拆子任务）
- CI 配置（术语黑名单检查、`now()` 静态检查、契约测试都还只是 PRD 里的要求）
- `/setup-matt-pocock-skills` 未跑，issue tracker 词表未配置
