# 交付物与推进循环

这一页是长期站着的作战简报。设定 `/goal` 后按它自主推进；每次准备停下来之前，用页尾「完成的定义」三个维度对一遍。

---

## 交付物

**一个能打开就用的网页产品。**

- 前端 Next.js，后端 FastAPI，数据库 PostgreSQL 18
- **一条命令起起来**（`docker compose up`），不用照着文档配环境
- **起来就有数据**：两万合成学生、8–12 个组织者、30–60 个招募。不是空库让人对着空白页发呆
- 能登录、能点、能给别人演示。不是截图，不是点不动的原型

十个可用页面（每屏的数据契约见 [`08-前端规格`](./08-前端规格.md) §3）：

```
想做点什么 · 这次让他们看到什么 · 等配队 · 给你找的人 · 这次你会得到什么
还差这几件事 · 项目空间 · 这次留下了什么
我的记录（我参加过的 / 关于我的记录 / 谁能看到我）· 招募（学生侧 + 组织者侧）
```

### 这个产品能做的八件事

1. 说一句话，系统整理成一张能改的需求卡
2. 逐项勾选这次露出什么，能预览别人看到的样子
3. 到配队窗口，收到 2–3 个小队，每队附**可逐条追溯**的理由，没有百分比
4. 被邀请的一侧看到"这次我会得到什么"，能有条件接受，也能零负担拒绝
5. 全员确认后共域在**一个事务内**诞生；未达门槛什么都不产生
6. 在项目空间里推进：任务、素材、决策门、角落里的助手
7. 结束后逐项确认，形成带来源和有效期的记录
8. 时钟快进两周，同一批人的新需求里出现"你们上次一起完成过 X"

第 8 条用 `SimulatedClock` 演示，**不需要真等两周**——这是产品能力的验证，不是对真实世界结果的承诺。

不在交付范围内的：多校园、外部 Agent 生态、学习排序、商业化、完整组织者工作台。

---

## 循环

设定 `/goal` 之后自主运行，逐 issue 实现并提交，不停下来问下一步做什么。

```
实现 milestone 的 issue（逐个提交）
        ↓
架构审视      用 codebase-design 词汇找 deepening 机会
        ↓
/verify       确认在轨且不冗余
        ↓
提交推送      提交信息说明这个里程碑改变了什么判断
        ↓
下一个里程碑
```

**只在一种情况下停下来找人**：某个决定如果做错会导致整个里程碑返工。其余决定自己定，写进 `docs/adr/`。

**排序按"哪部分决定产品能否成立"，不按数据流顺序。** AI 承重的四处是语义召回、
受限协商、行动回声、记忆切面抽取——拿掉它们产品就不成立；意图编译和证明转写
可以被表单和模板替代。详见 [ADR 0003](./adr/0003-where-ai-is-load-bearing.md)。

HANDOFF「待你决定」的 6 项属于自己定的范畴，不是阻塞项。

---

## 里程碑

| # | 名称 | Issue | 里程碑达成的判据 |
|---|---|---|---|
| **M0** | 地基 | #1 | 时间可快进；合成主体存在且不与真人同局；静态检查确认领域核心无 `now()` |
| **M1** | 表达与供给 | #2 #3 #4 #16 | 说一句话得到需求卡；逐项授权生效；组织者能发招募；念头能暂存并孵化 |
| **M2** | 撮合成组 | #5 #17 #6 #7 #15 | 两万合成人口跑通漏斗；**语义召回接上**（schema 装不下的长尾）；窗口清算产出 2–3 个带理由的小队；凑不出队时给阻塞说明 |
| **M3** | 成局与空间 | #8 #9 #10 #11 | 互惠视角 + 三档代聊；确认门原子成局；共域可用；**关掉助手后共域照常可用** |
| **M4** | 记忆与闭环 | #12 #13 #14 | 逐项确认形成记录；三个自我管理面可用；**闭环合上**——记录改变下一次成局证明 |

M4 结束后进入交付验收。

### 进度

| 里程碑 | 状态 | 实测 |
|---|---|---|
| M0 | ✅ | 领域纯度检查 13 个文件通过 |
| M1 | ✅ | 意图编译走规则解析器；逐项授权三档；念头暂存 |
| M2 | ✅ | 两万人漏斗 16ms；语义召回 **7 倍富集**（top-20 里 55% 命中，基线 7.8%）；求解 40 候选 6 组**中位 0.082s**；稳定性 5.4ms |
| M3 | ✅ | A2A Task 生命周期复用；确认门原子成局；共域关掉助手照常；能力令牌 + 策略纪元 |
| M4 | 进行中 | 表已建好（`evidence` / `memory_facets`）；行动回声与回流在做 |

**已建成的可运行部分**：`docker compose up` 起 db + api + web；`pnpm seed` 1.3 秒
播两万人 + 10 个组织 + 48 份招募；31 条 API 路径；CI 三个 job（含领域纯度、
契约无漂移、界面词汇三条静态检查）。

**已知未完成**：三个自我管理面（#13）、`e2e:persona-walkthrough` 量表、
十个页面里前端只做了四屏。

---

## 「在轨」的定义

`/verify` 每次必须回答这四条，任一为否即偏离：

1. **本里程碑所有 issue 的验收标准全绿**，不是"差不多了"
2. **13 条领域不变量测试全绿**（见 PRD Testing Decisions）
3. **没有引入 [`HANDOFF.md`](../HANDOFF.md) 「别重开」清单里的任何一项**
4. **闭环没有被为了赶进度而切断**——任何一片都不能以"先跳过回流"为代价通过

第 4 条是最容易失守的。前 13 片都在铺路，如果为了让某片好做而砍掉尾部，整个产品的论点就没了。

---

## 「不冗余」的定义

同样每次 `/verify` 检查，用 `codebase-design` 的词汇表述：

1. **没有为测试而造的假实现。** 唯一被替换的是"人"——真 Postgres、真 CP-SAT、真 pgvector、真 LLM（CI 走录制回放）
2. **没有第二数据源。** Postgres 是唯一权威，向量与图投影可随时重建
3. **没有手写类型。** Pydantic → OpenAPI 3.1 → 前端类型自动派生，三层同名
4. **没有为新场景新增的领域核心文件。** 场景差异落在行动类别注册表
5. **每个新 adapter 至少有两个真实调用点。** 一个 adapter 是假想的 seam，两个才是真的
6. **没有孤儿抽象。** 通不过删除测试的模块就删掉

---

## 交付验收：UI-only autoresearch

M4 通过后执行。**只通过界面操作，不读代码、不调 API、不看文档**——像一个刚打开这个网页的学生。

直接喂给 `/autoresearch` 的五项配置：

```
Goal:
  一个没读过任何文档的人，仅靠界面能独立走通「说一句话 → 拿到小队 →
  确认成局 → 推进项目 → 留下记录 → 被再次找到」全链路，
  并能在结束后用自己的话说出这个产品是干什么的。

Scope:
  仅限用户界面。禁止读源码、禁止直连 API、禁止查阅 docs/。
  运行在合成校园租户上（campus_id = simulation），
  真人 persona 与合成人口交互，合成主体带可见标记。

Direction:
  逐个 persona 走完全链路，记录每一次卡住、每一次看不懂、每一次想放弃：
  1. 大一新生，零历史，不知道这产品能干嘛
  2. 慢热的大三技术生，有能力但不想主动开口
  3. 赶 deadline 的发起人，只剩两天
  4. 社团组织者，要为比赛凑三支队
  5. 隐私敏感者，想知道系统记了自己什么、能不能删干净
  6. 收到邀请但想拒绝的人
  7. 中途要退出的成员
  8. 完成过一次、两周后回来的人

Metric:
  每个 persona 的：
    blocked_steps    卡住次数（无法继续或走错方向）
    confusion_hits   看不懂的文案条数
    abandon_points   会真的放弃的位置数
    term_leaks       界面上出现领域词汇的次数
    missing_states   五态缺失数（首次/加载/空/错误/降级）
  总分 = 五项加总，越低越好。终局目标：全 persona 总分 = 0。

Verify:
  pnpm e2e:persona-walkthrough --report
  输出 JSON，取 total_friction 字段作为可测数值。
  （这个脚本本身在 M4 内建好，它是终局验收的一部分，不是事后补的。）

Guard:
  pnpm test && pnpm typecheck
  不允许为了降低 friction 分而破坏任何领域不变量。
```

**判定**：任一 persona 在任一步骤上卡住，都算未通过。产品不是给读过 163 条用户故事的人用的。

**这一关必须能失败。** 如果 autoresearch 从来没判过不合格，说明量表设松了——`e2e:persona-walkthrough` 在 M4 内建好而不是事后补，就是为了避免它被写成"能跑通就行"的形状。

---

## 三条不可越过的约束

无论进度多紧：

1. **AI 可以代为表达，但不能代为承诺。** 承诺状态只接受真人签名的命令
2. **未获真人确认的提案不创建事件、共域或关系边**
3. **稳定性检查未通过的分区不得成为提案**

其余的一切——包括代聊是否披露、撮合窗口多长、稳定性用哪个概念——都是可配置策略，不是红线。

### 一条被删掉的约束，以及为什么

**时间曾经是硬约束，现在不是了**（[ADR 0006](./adr/0006-time-is-not-a-hard-constraint.md)）。

产品的形态就是：**发布组队，或者收到别人的邀请，确认加入就行。**不确认可能是时间不合适，也可能是别的——无所谓，那是他的表达。就算确认了，后面还可能因为没聊好而退出。

既然后面有真人这一关，前面就不该假装能算准，更不该为此加一套难处理的机制。撮合要做的是**把可能对得上的人放到一起**，不是替他们提前做决定。

---

## 起手

设定 `/goal`（内容见本页「交付物」与「完成的定义」两节），然后从 M0 · issue #1 开始，自主推进到 M4，最后跑交付验收。

## 完成的定义：三个维度全绿才算

### A. 完整——没有半条路

- [ ] 上面八件事端到端跑通，没有一步是演示用的假实现
- [ ] 十个页面全部可用
- [ ] 16 个 issue 的验收标准逐条全绿
- [ ] 每一屏五态齐全：首次 / 加载 / 空 / 错误 / 降级
- [ ] 13 条领域不变量都有测试且全绿
- [ ] 代码里没有 TODO、没有占位实现、没有"以后再说"的分支
- [ ] 降级路径真的能降级：LLM / 向量 / 实时 / 助手任一挂掉，用户仍能保存需求、看空间、人工确认、上传证据

### B. 可扩展——用测试证明，不用形容词

- [ ] 新增一个行动类别不改领域核心的任何一个文件（回归测试证明）
- [ ] 所有状态可通过版本化 API 完整获取；换一个前端不需要动后端
- [ ] 换 LLM、换向量后端、换求解器实现，领域测试一条都不用改
- [ ] Postgres 是唯一权威；向量与图投影删库能重建
- [ ] 容量护栏在两万人合成校园上实测过，估计值已替换为实测值
- [ ] 租户隔离有测试：合成主体不与真人同局，跨租户读取被拒

### C. 经真实检验——autoresearch 有权判不合格

- [ ] 8 个 persona 的 UI-only autoresearch 总摩擦分 = 0
- [ ] `e2e:persona-walkthrough` 量表在 M4 内建好，不是事后补的
- [ ] autoresearch 报出的每一个卡点都有对应的修复提交，没有被记成 issue 放过
- [ ] `pnpm test && pnpm typecheck` 全绿

**这一关不是盖章，是可以不通过的。** 失败就回到对应里程碑重做。
